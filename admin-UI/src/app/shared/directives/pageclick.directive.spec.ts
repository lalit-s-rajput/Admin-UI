import { PageclickDirective } from './pageclick.directive';

describe('PageclickDirective', () => {
  it('should create an instance', () => {
    const directive = new PageclickDirective();
    expect(directive).toBeTruthy();
  });
});
